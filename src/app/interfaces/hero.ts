export interface Hero {
    id: number,
    name: string,
    description: string,
    photo: string
}

export const DEFAULT_HERO: Hero = {
    id: null,
    name: null,
    description: null,
    photo: null
}